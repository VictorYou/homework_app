apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - ingress.yaml
  - service.yaml
configMapGenerator:
  - name: hello-app
    literals:
      - POSTGRES_MAX_POOL_SIZE="4"
generatorOptions:
  immutable: true
